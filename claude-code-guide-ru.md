# Полное руководство по Claude Code

## Введение

Claude Code — это агентный инструмент для программирования от Anthropic, который работает прямо в терминале. Он понимает вашу кодовую базу и помогает программировать быстрее: выполняет рутинные задачи, объясняет сложный код, управляет git-процессами — и всё это через команды на естественном языке.

---

## Содержание

### Быстрый старт
1. [Системные требования](#системные-требования)
2. [Установка](#установка)
3. [Аутентификация](#аутентификация)
4. [Первый запуск](#первый-запуск)

### Основное использование
5. [Основные команды CLI](#основные-команды-cli)
6. [Slash-команды](#slash-команды)
7. [Работа с файлами и контекстом](#работа-с-файлами-и-контекстом)
8. [Файл CLAUDE.md](#файл-claudemd)

### Продвинутые возможности
9. [Пользовательские команды](#пользовательские-команды)
10. [Управление разрешениями](#управление-разрешениями)
11. [Интеграция с Git](#интеграция-с-git)
12. [MCP-серверы](#mcp-серверы)
13. [Субагенты](#субагенты)
14. [Хуки](#хуки)

### Практическое применение
15. [Лучшие практики](#лучшие-практики)
16. [Примеры из реальной жизни](#примеры-из-реальной-жизни)
17. [Оптимизация производительности](#оптимизация-производительности)
18. [Горячие клавиши](#горячие-клавиши)

### Справка
19. [Часто задаваемые вопросы (FAQ)](#часто-задаваемые-вопросы-faq)
20. [Устранение неполадок](#устранение-неполадок)

---

## Системные требования

| Параметр | Требование |
|----------|------------|
| **ОС** | macOS 10.15+, Ubuntu 20.04+/Debian 10+, Windows 10+ (через WSL) |
| **Оперативная память** | 4 ГБ минимум |
| **Node.js** | Версия 18 или выше |
| **Сеть** | Требуется подключение к интернету |
| **Оболочка** | Bash, Zsh или Fish |
| **Регион** | Страна, поддерживаемая Anthropic |

---

## Установка

### Стандартная установка через npm (рекомендуется)

```bash
# Установка Claude Code глобально
npm install -g @anthropic-ai/claude-code

# Проверка установки
claude --version
```

> ⚠️ **Важно:** Не используйте `sudo` при установке через npm — это может привести к проблемам с правами доступа.

### Установка на Windows

Claude Code не работает напрямую в Windows. Необходимо установить WSL (Windows Subsystem for Linux):

```powershell
# В PowerShell от имени администратора
wsl --install
```

После перезагрузки установите Node.js и Claude Code в WSL:

```bash
# В терминале Ubuntu/WSL
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
npm install -g @anthropic-ai/claude-code
```

### Проверка установки

```bash
# Запуск диагностики
claude doctor
```

---

## Аутентификация

При первом запуске Claude Code вам будет предложено выбрать способ аутентификации:

### Вариант 1: Claude Pro/Max подписка (рекомендуется)

Единая подписка для веб-интерфейса Claude и Claude Code. При запуске выберите опцию аутентификации через браузер.

### Вариант 2: Anthropic Console (API)

Оплата по факту использования через API-ключ:

```bash
# Установка API-ключа через переменную окружения
export ANTHROPIC_API_KEY="sk-xxxxxxxxxxxxxxxxxxxxxxxx"

# Добавьте в ~/.bashrc или ~/.zshrc для постоянного использования
echo 'export ANTHROPIC_API_KEY="your-api-key"' >> ~/.bashrc
source ~/.bashrc
```

Или через файл настроек:

```bash
# Создайте файл настроек
mkdir -p ~/.claude
cat > ~/.claude/settings.json << EOF
{
  "env": {
    "ANTHROPIC_API_KEY": "sk-xxxxxxxxxxxxxxxxxxxxxxxx"
  }
}
EOF
```

### Смена аккаунта

```bash
# В интерактивном режиме используйте команду
/login
```

---

## Первый запуск

```bash
# Перейдите в директорию проекта
cd /path/to/your/project

# Запустите Claude Code
claude
```

Claude Code автоматически читает файлы по мере необходимости — вам не нужно вручную добавлять контекст.

### Примеры первых запросов

```
> объясни архитектуру этого проекта
> найди и исправь баг в файле auth.py
> добавь тесты для модуля user service
> создай документацию для API
```

---

## Основные команды CLI

### Запуск и режимы работы

```bash
# Интерактивный режим (по умолчанию)
claude

# Headless-режим (вывод результата в терминал)
claude -p "твой запрос"

# Выбор модели
claude --model sonnet    # Sonnet — быстрый и эффективный
claude --model opus      # Opus — максимальные возможности

# Добавление директорий в контекст
claude --add-dir ../frontend --add-dir ../backend

# Вывод в JSON-формате
claude -p "сгенерируй API документацию" --output-format json

# Режим без запросов разрешений (используйте осторожно!)
claude --dangerously-skip-permissions
```

### Работа с пайпами

```bash
# Анализ вывода другой команды
git log --oneline -n 10 | claude -p "создай release notes из этих коммитов"

# Анализ файла
cat data.csv | claude -p "кто выиграл больше всего игр?"

# Анализ ошибок
npm test 2>&1 | claude -p "объясни эти ошибки и предложи решения"
```

---

## Slash-команды

Slash-команды вводятся прямо в интерактивном режиме Claude Code.

### Основные команды

| Команда | Описание |
|---------|----------|
| `/help` | Показать все доступные команды |
| `/init` | Создать файл CLAUDE.md с описанием проекта |
| `/clear` | Очистить историю диалога и контекст |
| `/compact` | Сжать контекст, сохранив важную информацию |
| `/compact [инструкция]` | Сжать с указанием, что сохранить |
| `/exit` или `/quit` | Выйти из Claude Code |

### Управление сессией

| Команда | Описание |
|---------|----------|
| `/resume` | Возобновить предыдущую сессию |
| `/rewind` | Откатить изменения к предыдущей точке |
| `/export` | Экспортировать разговор |
| `/login` | Сменить аккаунт |

### Настройки и диагностика

| Команда | Описание |
|---------|----------|
| `/config` | Открыть настройки Claude Code |
| `/permissions` | Управление разрешениями инструментов |
| `/model` | Переключить модель (Opus/Sonnet) |
| `/cost` | Показать использование токенов |
| `/context` | Визуализировать использование контекста |
| `/doctor` | Запустить диагностику установки |
| `/terminal-setup` | Настроить терминал для Claude Code |

### Интеграции

| Команда | Описание |
|---------|----------|
| `/mcp` | Управление MCP-серверами |
| `/hooks` | Настроить хуки |
| `/agents` | Управление субагентами |
| `/install-github-app` | Установить GitHub App для ревью PR |

### Выполнение bash-команд

Используйте `!` для выполнения bash-команд прямо в сессии:

```
!git status
!npm run test
!ls -la
```

---

## Работа с файлами и контекстом

### Ссылки на файлы

- Используйте `@filename` для ссылки на файл
- Используйте `Tab` для автодополнения путей
- Перетаскивайте файлы с зажатым `Shift` для добавления в контекст

### Работа с изображениями

- Вставляйте изображения через `Control+V` (не Command+V)
- Можно добавлять скриншоты дизайна, диаграммы, примеры UI

### Управление контекстом

```
# Очистить контекст для новой задачи
/clear

# Сжать контекст с сохранением важного
/compact Сохрани реализацию аутентификации и схему базы данных

# Посмотреть использование контекста
/context
```

---

## Файл CLAUDE.md

CLAUDE.md — это специальный файл в корне проекта, который Claude Code читает для понимания контекста.

### Создание файла

```
/init
```

### Структура файла

```markdown
# Название проекта

## Описание
Краткое описание проекта и его назначения.

## Технологический стек
- Frontend: React, TypeScript
- Backend: Node.js, Express
- База данных: PostgreSQL

## Структура проекта
- `/src` - исходный код
- `/tests` - тесты
- `/docs` - документация

## Команды разработки
- `npm run dev` - запуск dev-сервера
- `npm run test` - запуск тестов
- `npm run build` - сборка проекта

## Соглашения о коде
- Используем ESLint с конфигом Airbnb
- Все функции должны иметь JSDoc комментарии
- Тесты обязательны для новой функциональности

## Важные заметки
- API-ключи хранятся в .env файле
- Перед коммитом запускаем `npm run lint`
```

> 💡 **Совет:** Закоммитьте CLAUDE.md в репозиторий, чтобы вся команда использовала одинаковый контекст.

---

## Пользовательские команды

Создавайте собственные slash-команды для часто используемых задач.

### Проектные команды

Доступны только в текущем проекте и могут быть расшарены через репозиторий.

```bash
# Создаём директорию для команд
mkdir -p .claude/commands

# Создаём команду /review
cat > .claude/commands/review.md << 'EOF'
Выполни комплексный код-ревью последних изменений:

1. Проверь соответствие стандартам TypeScript и React
2. Убедись в правильной обработке ошибок
3. Проверь доступность (a11y)
4. Оцени покрытие тестами
5. Проверь на уязвимости безопасности
6. Оцени производительность

Обнови CLAUDE.md если нашёл новые паттерны.
EOF
```

### Персональные команды

Доступны во всех ваших проектах.

```bash
# Создаём директорию для персональных команд
mkdir -p ~/.claude/commands

# Создаём команду /security-check
cat > ~/.claude/commands/security-check.md << 'EOF'
Проверь этот код на уязвимости безопасности:
- SQL-инъекции
- XSS-атаки
- Проблемы с аутентификацией
- Утечки чувствительных данных
EOF
```

### Использование аргументов

```markdown
---
argument-hint: [номер-issue] [приоритет]
description: Исправить GitHub issue
---
Исправь issue #$1 с приоритетом $2.

Шаги:
1. Получи детали issue через `gh issue view $1`
2. Проанализируй проблему
3. Найди соответствующие файлы
4. Реализуй исправление
5. Создай коммит с описательным сообщением
```

Использование:
```
/fix-issue 42 high
```

---

## Управление разрешениями

Claude Code запрашивает разрешения перед выполнением действий.

### Настройка через файл

Создайте `.claude/settings.json` в проекте:

```json
{
  "permissions": {
    "allowedTools": [
      "Read",
      "Write(src/**)",
      "Bash(git *)",
      "Bash(npm *)"
    ],
    "deny": [
      "Read(.env*)",
      "Write(production.config.*)",
      "Bash(rm *)",
      "Bash(sudo *)"
    ]
  }
}
```

### Интерактивная настройка

```
/permissions
```

### Режим автоматического принятия

```bash
# Автоматически принимать все изменения (Shift+Tab для переключения)
# Или через командную строку:
claude --dangerously-skip-permissions
```

> ⚠️ **Внимание:** Используйте этот режим осторожно, только если уверены в безопасности окружения.

---

## Интеграция с Git

Claude Code отлично работает с Git.

### Примеры команд

```
> создай новую ветку feature-auth
> сделай коммит с описательным сообщением
> покажи изменения с последнего коммита
> создай pull request
> разреши конфликты слияния
> сгенерируй release notes из последних 10 коммитов
```

### Автоматический ревью PR

```
/install-github-app
```

После установки Claude будет автоматически ревьюить ваши PR.

### Настройка ревью

Отредактируйте файл `claude-code-review.yml`:

```yaml
prompt: |
  Проверяй только:
  - Критические баги
  - Уязвимости безопасности
  - Логические ошибки
  
  Не комментируй:
  - Стиль именования переменных
  - Форматирование
  - Незначительные улучшения
```

---

## MCP-серверы

MCP (Model Context Protocol) позволяет Claude Code взаимодействовать с внешними инструментами и сервисами.

### Управление MCP-серверами

```bash
# Добавить MCP-сервер
claude mcp add <имя> <команда> [аргументы...]

# Список установленных серверов
claude mcp list

# Получить информацию о сервере
claude mcp get <имя>

# Удалить сервер
claude mcp remove <имя>
```

### Примеры

```bash
# Добавить Puppeteer для автоматизации браузера
claude mcp add puppeteer -- npx -y puppeteer-mcp-server

# Добавить Airtable
claude mcp add --transport stdio airtable \
  --env AIRTABLE_API_KEY=YOUR_KEY \
  -- npx -y airtable-mcp-server
```

### Конфигурация в файле проекта

Создайте `.mcp.json` в корне проекта:

```json
{
  "mcpServers": {
    "puppeteer": {
      "command": "npx",
      "args": ["-y", "puppeteer-mcp-server"]
    },
    "sentry": {
      "command": "npx",
      "args": ["-y", "sentry-mcp-server"],
      "env": {
        "SENTRY_DSN": "${SENTRY_DSN}"
      }
    }
  }
}
```

---

## Субагенты

Субагенты — это специализированные ИИ-ассистенты с собственными инструкциями и контекстом.

### Создание субагента

```
/agents
```

Или создайте файл `.claude/agents/code-reviewer.md`:

```markdown
---
name: code-reviewer
description: Эксперт по код-ревью. Используй автоматически после изменений кода.
tools: Read, Grep, Glob, Bash
model: sonnet
---

Ты — старший код-ревьюер. Фокусируйся на:
- Качестве кода
- Безопасности
- Лучших практиках
- Производительности

Всегда предоставляй конкретные рекомендации с примерами кода.
```

### Вызов субагента

```
@code-reviewer проверь изменения в auth модуле
```

Или через CLI:

```bash
claude --agents '{
  "code-reviewer": {
    "description": "Эксперт по код-ревью",
    "prompt": "Ты старший код-ревьюер. Фокусируйся на качестве и безопасности.",
    "tools": ["Read", "Grep", "Glob", "Bash"],
    "model": "sonnet"
  }
}'
```

---

## Хуки

Хуки — это shell-команды, которые выполняются автоматически в определённые моменты.

### Настройка хуков

```
/hooks
```

Или в `.claude/settings.json`:

```json
{
  "hooks": {
    "preEditAccept": [
      "prettier --write $FILE"
    ],
    "postEditAccept": [
      "npx tsc --noEmit $FILE"
    ],
    "sessionStart": [
      "git fetch origin"
    ]
  }
}
```

### Типы хуков

- `sessionStart` — при старте сессии
- `preEditAccept` — перед принятием изменений в файл
- `postEditAccept` — после принятия изменений
- `stop` — при остановке цикла

---

## Лучшие практики

### 1. Управление контекстом

#### Очищайте контекст при переключении между задачами

```
# После завершения задачи
/clear
```

#### Используйте `/compact` стратегически

```
# Перед переполнением контекста
/compact Сохрани текущую реализацию авторизации, схему БД и API endpoints

# После большого рефакторинга
/compact Сохрани только финальное решение, удали промежуточные попытки
```

#### Разбивайте большие задачи на части

Вместо:
```
> Реализуй полную систему e-commerce с корзиной, оплатой, доставкой и админ-панелью
```

Делайте поэтапно:
```
> Реализуй базовую модель Product и каталог
/compact
> Добавь корзину покупок
/compact
> Интегрируй платежную систему
```

### 2. Эффективная коммуникация

#### Начинайте с общего, переходите к конкретному

```
# Сначала общее видение
> Хочу добавить систему уведомлений в приложение

# Claude предложит архитектуру, затем спросите:
> Начнём с email-уведомлений для новых заказов
```

#### Будьте конкретны в запросах

❌ Плохо:
```
> исправь баг
> оптимизируй код
> добавь тесты
```

✅ Хорошо:
```
> В файле @api/orders.ts функция createOrder не валидирует inventory перед созданием заказа. Добавь проверку и верни ошибку 400 если товара нет в наличии

> Функция calculateShipping в @utils/shipping.ts выполняется 200ms. Оптимизируй используя кеширование для стандартных маршрутов

> Добавь integration тесты для auth flow: регистрация → подтверждение email → вход → обновление токена
```

#### Предоставляйте контекст при ошибках

```
> Пользователи не могут войти в систему. Вот что происходит:
>
> Ошибка: "Invalid credentials"
> Файл: @api/auth.ts:45
> Stack trace: [вставьте полный trace]
>
> Ожидаемое: редирект на /dashboard после логина
> Фактическое: остаёмся на /login с ошибкой 401
>
> Данные для теста: email: test@example.com, password: правильный пароль
> База данных: пользователь существует, пароль совпадает
```

### 3. Работа с файлами

#### Используйте @ для ссылок на файлы

```
> Прочитай @api/users.ts и @tests/users.test.ts и найди, почему тест падает
```

#### Указывайте конкретные файлы для изменения

```
> Измени только @components/Header.tsx, не трогай другие компоненты
```

#### Используйте Shift+перетаскивание для добавления файлов

Это быстрее, чем печатать пути к файлам.

### 4. Git и управление версиями

#### Коммитьте небольшими порциями

```
> Закоммить изменения в auth модуле
> [Claude коммитит]
> Теперь добавь тесты
> [Claude добавляет тесты]
> Закоммить тесты отдельным коммитом
```

#### Используйте описательные сообщения

Claude автоматически создаёт хорошие commit messages, но вы можете направить его:

```
> Закоммить с сообщением в стиле conventional commits
> Закоммить с подробным описанием изменений
```

#### Проверяйте изменения перед коммитом

```
> Покажи что изменилось
!git diff
> Хорошо, теперь закоммить
```

### 5. Тестирование и качество кода

#### Всегда просите добавить тесты

```
> Реализуй функцию calculateDiscount и добавь unit тесты
```

#### Запускайте тесты после изменений

```
> Запусти тесты и исправь падающие
```

Claude будет:
1. Запускать тесты
2. Анализировать ошибки
3. Исправлять код
4. Перезапускать тесты
5. Повторять до успеха

#### Просите о code review

```
> Сделай ревью изменений в @api/payments.ts на предмет:
> - Безопасности
> - Обработки ошибок
> - Производительности
> - Читаемости кода
```

### 6. Использование продвинутых возможностей

#### Создавайте переиспользуемые команды

Для частых задач создайте slash-команды:

```bash
# .claude/commands/deploy-staging.md
Выполни деплой на staging:
1. Запусти тесты
2. Собери проект
3. Проверь переменные окружения
4. Задеплой на staging
5. Запусти smoke tests
6. Уведоми в Slack
```

Использование:
```
/deploy-staging
```

#### Используйте субагентов для специализации

```
# Для быстрого написания тестов
@test-writer создай тесты для всех функций в @utils/validators.ts

# Для code review
@code-reviewer проверь PR #123
```

#### Настройте хуки для автоматизации

```json
{
  "hooks": {
    "preEditAccept": ["prettier --write $FILE"],
    "postEditAccept": ["eslint $FILE --fix"]
  }
}
```

### 7. Безопасность

#### Никогда не коммитьте секреты

```
> Используй переменные окружения для API ключей, не хардкодь их
```

#### Используйте .claudeignore

```
# .claudeignore
.env
.env.*
*.key
*.pem
secrets/
node_modules/
```

#### Настройте разрешения

```json
{
  "permissions": {
    "deny": [
      "Bash(rm -rf *)",
      "Bash(sudo *)",
      "Write(.env*)",
      "Write(secrets/*)"
    ]
  }
}
```

### 8. Отладка и решение проблем

#### Прерывайте неправильные действия

```
# Нажмите Escape если Claude делает что-то не то
Escape

# Затем уточните
> Стоп, не удаляй этот файл. Я хотел только прочитать его
```

#### Используйте /rewind для отката

```
# Если что-то пошло не так
Escape (дважды)

# Или
/rewind
```

#### Просите объяснений

```
> Объясни почему ты выбрал именно такое решение
> Какие есть альтернативы?
> Какие могут быть проблемы с этим подходом?
```

### 9. Оптимизация производительности

#### Выбирайте правильную модель

```bash
# Простые задачи → Sonnet (быстрее, дешевле)
claude --model sonnet

# Сложные архитектурные решения → Opus
claude --model opus
```

#### Используйте headless-режим для автоматизации

```bash
# В скриптах и CI/CD
git diff main | claude -p "найди потенциальные баги" > report.txt
```

#### Создайте качественный CLAUDE.md

Хороший CLAUDE.md экономит токены и время:

```markdown
# Проект: E-commerce API

## Стек
- Node.js 18, Express, TypeScript
- PostgreSQL + Prisma ORM
- Redis для кеширования
- Jest для тестов

## Структура
- `/src/api` - REST endpoints
- `/src/services` - бизнес-логика
- `/src/db` - модели и миграции

## Важные правила
- Все endpoints требуют JWT аутентификацию
- Используем репозиторный паттерн
- Ошибки через централизованный handler
- Все публичные функции должны иметь JSDoc
- Тесты обязательны для новой функциональности

## Не менять
- `/src/legacy` - удалим в следующем релизе
```

### 10. Работа в команде

#### Шарьте конфигурацию через репозиторий

Закоммитьте в git:
- `CLAUDE.md`
- `.claude/commands/`
- `.claude/agents/`
- `.claudeignore`

#### Документируйте решения

```
> Добавь комментарий объясняющий почему мы используем именно этот подход
```

#### Используйте /export для шаринга

```
/export
# Поделитесь экспортом с коллегами для передачи контекста
```

---

## Примеры из реальной жизни

### Пример 1: Рефакторинг устаревшего кода

```
> Я хочу отрефакторить этот legacy модуль auth.js, чтобы использовать современные практики

Шаги Claude:
1. Читает auth.js и зависимости
2. Анализирует текущую архитектуру
3. Предлагает план рефакторинга
4. Реализует изменения поэтапно
5. Создает тесты для проверки функциональности
6. Коммитит изменения с понятным описанием
```

### Пример 2: Исправление бага в продакшене

```
> У нас баг в продакшене: пользователи не могут войти. Вот логи из Sentry:
> [вставляете stack trace]

Действия Claude:
1. Анализирует stack trace
2. Находит проблемный код
3. Определяет корневую причину
4. Предлагает исправление
5. Создает тест для предотвращения регрессии
6. Генерирует hotfix-ветку и PR
```

### Пример 3: Создание новой функции с нуля

```
> Нужно добавить экспорт данных в CSV для админ-панели

Процесс работы:
1. Claude читает существующую структуру админ-панели
2. Определяет, где добавить новую функцию
3. Создает endpoint для экспорта
4. Реализует логику генерации CSV
5. Добавляет кнопку в UI
6. Пишет unit и integration тесты
7. Обновляет документацию
```

### Пример 4: Ревью и оптимизация производительности

```
> Наша страница каталога продуктов загружается слишком медленно

Claude анализирует:
1. Читает компонент каталога
2. Находит неэффективные запросы к БД (N+1 проблема)
3. Выявляет отсутствие пагинации
4. Обнаруживает рендеринг всех элементов сразу
5. Предлагает решения:
   - Добавить индексы в БД
   - Реализовать пагинацию
   - Использовать виртуализацию списка
   - Добавить кеширование
6. Реализует оптимизации
7. Измеряет улучшение производительности
```

### Пример 5: Миграция на новую версию библиотеки

```
> Нужно обновить React с версии 17 на 18

Claude выполняет:
1. Проверяет текущую версию и зависимости
2. Читает changelog React 18
3. Обновляет package.json
4. Находит breaking changes в коде
5. Обновляет код для совместимости (createRoot, Suspense, и т.д.)
6. Запускает тесты и исправляет падающие
7. Обновляет документацию
```

### Пример 6: Настройка CI/CD pipeline

```
> Настрой GitHub Actions для автоматического деплоя

Claude создает:
1. Файл .github/workflows/deploy.yml
2. Конфигурирует этапы:
   - Установка зависимостей
   - Линтинг
   - Тесты
   - Сборка
   - Деплой на staging/production
3. Добавляет секреты и переменные окружения
4. Настраивает уведомления о статусе деплоя
5. Тестирует pipeline на тестовой ветке
```

---

## Оптимизация производительности

### Управление размером контекста

Claude Code имеет ограничение на размер контекста (обычно 200K токенов). Вот как оптимизировать использование:

#### 1. Используйте `/compact` стратегически

```
# Перед началом новой большой задачи
/compact Сохрани архитектуру API, схему базы данных и конфигурацию аутентификации

# После завершения большой задачи
/compact Сохрани только финальное решение и важные решения по архитектуре
```

#### 2. Очищайте контекст между задачами

```
# После завершения задачи
/clear

# Или используйте rewind для отката к конкретной точке
Escape (дважды)
```

#### 3. Разбивайте большие задачи

Вместо:
```
> Реализуй полную систему аутентификации с OAuth, JWT, 2FA, восстановлением пароля и email-уведомлениями
```

Используйте:
```
> Реализуй базовую JWT аутентификацию
# После завершения
/compact
> Добавь OAuth провайдеры (Google, GitHub)
# После завершения
/compact
> Добавь 2FA
```

### Ускорение работы

#### 1. Используйте правильную модель

```bash
# Для простых задач - Sonnet (быстрее и дешевле)
claude --model sonnet -p "исправь отступы в этом файле"

# Для сложных задач - Opus (медленнее, но умнее)
claude --model opus -p "оптимизируй архитектуру этого микросервиса"
```

#### 2. Используйте headless-режим для автоматизации

```bash
# Быстрый анализ без интерактивного режима
git diff | claude -p "найди потенциальные баги в этих изменениях" > review.txt
```

#### 3. Предоставляйте конкретный контекст

Вместо:
```
> Исправь баг
```

Используйте:
```
> В файле @api/users.ts функция getUserById возвращает null для существующих пользователей. Вот тест, который падает: @tests/users.test.ts
```

### Оптимизация для больших кодовых баз

#### 1. Используйте .claudeignore

Создайте файл `.claudeignore` для исключения ненужных файлов:

```
# .claudeignore
node_modules/
dist/
build/
.next/
coverage/
*.log
*.lock
.git/
.DS_Store
*.min.js
*.map
```

#### 2. Создайте хороший CLAUDE.md

Хороший CLAUDE.md уменьшает необходимость в чтении множества файлов:

```markdown
# Архитектура проекта

## Ключевые модули
- `src/auth/` - аутентификация (используем Passport.js)
- `src/api/` - REST API endpoints
- `src/db/` - модели базы данных (Prisma ORM)

## Важные паттерны
- Все API endpoints должны использовать middleware auth.js
- Ошибки обрабатываются через централизованный error handler
- Используем репозиторный паттерн для работы с БД

## Не трогать
- `src/legacy/` - старый код, который скоро удалим
```

#### 3. Используйте субагентов для специализированных задач

```markdown
# .claude/agents/test-writer.md
---
name: test-writer
description: Эксперт по написанию тестов
tools: Read, Write, Bash
model: sonnet
---

Ты специализируешься на написании тестов. Используй Jest и React Testing Library.
Следуй паттернам из существующих тестов в проекте.
```

Затем:
```
@test-writer напиши тесты для компонента UserProfile
```

---

## Горячие клавиши

| Клавиша | Действие |
|---------|----------|
| `Enter` | Отправить сообщение |
| `Escape` | Прервать текущую операцию |
| `Escape` (дважды) | Rewind — откат к предыдущей точке |
| `Tab` | Автодополнение файлов и команд |
| `Shift+Tab` | Переключить режим автоматического принятия |
| `Control+V` | Вставить изображение |
| `Control+C` | Выход из Claude Code |
| `Control+Z` | Приостановить Claude Code (возобновить: `fg`) |
| `Control+U` | Отменить ввод |
| `Control+B` | Запустить команду в фоне |
| `↑` / `↓` | Навигация по истории сообщений |

---

## Часто задаваемые вопросы (FAQ)

### Общие вопросы

#### В чем разница между Claude Code и GitHub Copilot?

Claude Code — это **агентный инструмент**, который может:
- Читать и понимать всю кодовую базу
- Выполнять bash-команды (git, npm, тестирование)
- Создавать и редактировать несколько файлов
- Работать с внешними инструментами через MCP
- Управлять всем workflow от задачи до коммита

GitHub Copilot — это **ассистент автодополнения**, который:
- Предлагает дополнения кода в редакторе
- Работает построчно/блоками
- Не выполняет команды
- Не управляет git-процессом

#### Могу ли я использовать Claude Code бесплатно?

Есть два варианта:
1. **Claude Pro/Max подписка** ($20-30/месяц) — включает веб-интерфейс и Claude Code
2. **API (pay-as-you-go)** — оплата только за использование, но может быть дороже при частом использовании

#### Безопасно ли использовать Claude Code с приватным кодом?

Да. Anthropic не использует ваши данные для обучения моделей. Весь код обрабатывается конфиденциально. Подробнее в [политике конфиденциальности Anthropic](https://www.anthropic.com/privacy).

#### Какие языки программирования поддерживает Claude Code?

Claude Code работает со всеми популярными языками:
- JavaScript/TypeScript, Python, Java, C++, C#, Go, Rust
- Ruby, PHP, Swift, Kotlin, Scala
- HTML/CSS, SQL, Shell scripts
- И многие другие

### Работа с проектами

#### Как Claude Code понимает мой проект?

Claude Code:
1. Читает файлы по мере необходимости (не загружает всё сразу)
2. Использует файл `CLAUDE.md` для контекста проекта
3. Анализирует структуру директорий
4. Читает конфигурационные файлы (package.json, requirements.txt и т.д.)

#### Могу ли я работать с несколькими проектами одновременно?

Да, используйте флаг `--add-dir`:

```bash
claude --add-dir ../frontend --add-dir ../backend
```

#### Как ограничить доступ Claude Code к определённым файлам?

Создайте `.claudeignore`:

```
.env
secrets/
*.key
production.config.js
```

### Управление контекстом

#### Что делать, когда контекст переполняется?

1. Используйте `/compact` для сжатия контекста
2. Используйте `/clear` для полной очистки
3. Разбивайте большие задачи на меньшие
4. Создайте хороший `CLAUDE.md` для уменьшения необходимости читать много файлов

#### Сохраняется ли история между сессиями?

Да, используйте `/resume` для возобновления предыдущей сессии:

```
/resume
```

#### Как экспортировать разговор?

```
/export
```

### Интеграция с инструментами

#### Как добавить интеграцию с базой данных?

Используйте MCP-серверы. Примеры:

```bash
# PostgreSQL
claude mcp add postgres -- npx -y @modelcontextprotocol/server-postgres \
  --env DATABASE_URL=postgresql://user:pass@localhost/db

# MongoDB
claude mcp add mongo -- npx -y @modelcontextprotocol/server-mongodb \
  --env MONGODB_URI=mongodb://localhost:27017/mydb
```

#### Можно ли интегрировать с Jira/Linear/другими таск-трекерами?

Да, если есть MCP-сервер для вашего инструмента. Проверьте [MCP marketplace](https://github.com/modelcontextprotocol/servers) или создайте свой.

#### Как использовать Claude Code с Docker?

```bash
# Claude может работать с Docker напрямую
> запусти docker-compose up для dev окружения
> покажи логи из контейнера app
> перезапусти сервис database
```

### Git и разработка

#### Может ли Claude Code автоматически создавать PR?

Да:

```
> создай pull request для этих изменений
```

Claude автоматически:
1. Создаст ветку (если нужно)
2. Закоммитит изменения
3. Запушит в remote
4. Создаст PR с описанием

#### Как настроить автоматический код-ревью?

```
/install-github-app
```

После установки Claude будет автоматически ревьюить ваши PR на GitHub.

#### Может ли Claude разрешать конфликты слияния?

Да:

```
> разреши конфликты слияния в этой ветке
```

### Производительность и стоимость

#### Как уменьшить стоимость использования API?

1. Используйте модель Sonnet для простых задач
2. Используйте `/compact` и `/clear` часто
3. Создайте хороший `.claudeignore`
4. Пишите конкретные запросы, чтобы избежать лишних итераций
5. Используйте headless-режим для автоматизации

#### Как проверить, сколько токенов я использовал?

```
/cost
```

#### Почему Claude Code медленно работает?

Возможные причины:
1. Большая кодовая база — используйте `.claudeignore`
2. Переполненный контекст — используйте `/compact`
3. Использование модели Opus — переключитесь на Sonnet для простых задач
4. Проблемы с сетью — проверьте соединение

### Кастомизация

#### Могу ли я изменить поведение Claude?

Да, через:
1. `CLAUDE.md` — описание проекта и соглашений
2. Пользовательские команды — `.claude/commands/`
3. Субагенты — `.claude/agents/`
4. Хуки — `.claude/settings.json`

#### Как создать шаблон проекта для команды?

Создайте репозиторий-шаблон с:
- `CLAUDE.md` — описание стандартов команды
- `.claude/commands/` — общие команды
- `.claude/agents/` — стандартные агенты
- `.claudeignore` — стандартные исключения

#### Можно ли использовать Claude Code в CI/CD?

Да, в headless-режиме:

```bash
# В GitHub Actions
- name: Review code
  run: |
    git diff main | claude -p "проверь на баги и уязвимости" > review.txt
    cat review.txt
```

### Безопасность

#### Как предотвратить случайное удаление файлов?

1. Используйте настройки разрешений в `.claude/settings.json`:

```json
{
  "permissions": {
    "deny": [
      "Bash(rm *)",
      "Bash(sudo *)",
      "Write(.env*)"
    ]
  }
}
```

2. Не используйте `--dangerously-skip-permissions` в продакшене

#### Может ли Claude читать мои приватные ключи?

По умолчанию — да, если они в проекте. Защитите их через `.claudeignore`:

```
.env
.env.*
*.key
*.pem
secrets/
credentials/
```

#### Как работать с секретами в проекте?

1. Храните секреты в `.env` и добавьте в `.claudeignore`
2. Используйте переменные окружения
3. Используйте менеджеры секретов (AWS Secrets Manager, Vault)
4. Попросите Claude использовать плейсхолдеры:

```
> используй переменные окружения вместо хардкода для API ключей
```

### Проблемы и ошибки

#### Claude не выполняет то, что я прошу

Попробуйте:
1. Будьте более конкретны
2. Разбейте задачу на шаги
3. Предоставьте примеры
4. Покажите текущий код
5. Объясните ожидаемое поведение

Вместо:
```
> исправь это
```

Используйте:
```
> В файле @api/users.ts функция getUserById должна возвращать объект пользователя, но возвращает null. Вот тест: @tests/users.test.ts. Исправь функцию.
```

#### Claude изменил не те файлы

1. Нажмите `Escape` для прерывания
2. Используйте `/rewind` для отката
3. Будьте более конкретны:

```
> измени только файл auth.ts, не трогай другие файлы
```

#### Как отменить последние изменения Claude?

```
# Интерактивно
Escape (дважды)

# Или через команду
/rewind

# Или через git
!git restore .
```

---

## Устранение неполадок

### Диагностика

```bash
claude doctor
```

### Частые проблемы

#### Ошибка аутентификации

```bash
# Переаутентификация
/login

# Или проверьте API-ключ
echo $ANTHROPIC_API_KEY
```

#### Проблемы с сетью

```bash
# Настройка прокси
export HTTP_PROXY=http://proxy.example.com:8080
export HTTPS_PROXY=http://proxy.example.com:8080
```

#### Node.js не найден

```bash
# Проверка версии Node.js
node --version  # Должно быть 18+

# Установка через nvm
nvm install 18
nvm use 18
```

#### Медленный поиск в большой кодовой базе

Убедитесь, что установлен `ripgrep`:

```bash
# macOS
brew install ripgrep

# Ubuntu
sudo apt install ripgrep
```

### Логи и отладка

```bash
# Запуск в режиме отладки
claude --debug
```

### Сообщить о проблеме

```
/bug
```

---

## Полезные ресурсы

- **Официальная документация:** https://docs.claude.com/claude-code
- **GitHub репозиторий:** https://github.com/anthropics/claude-code
- **Discord сообщество:** Claude Developers Discord
- **Marketplace плагинов:** https://claudemarket.com

---

---

## Заключение

Claude Code — мощный инструмент, который превращает терминал в интеллектуальную среду разработки. Ключ к эффективному использованию:

1. **Начинайте с `/init`** — дайте Claude понять ваш проект
2. **Общайтесь естественно** — описывайте, что хотите сделать
3. **Управляйте контекстом** — используйте `/clear` и `/compact`
4. **Автоматизируйте рутину** — создавайте пользовательские команды
5. **Экспериментируйте** — найдите свой идеальный workflow

### Дополнительные ресурсы

- **Официальная документация (EN):** https://docs.claude.com/claude-code
- **GitHub репозиторий:** https://github.com/anthropics/claude-code
- **MCP серверы:** https://github.com/modelcontextprotocol/servers
- **Сообщество:** Discord, Reddit r/ClaudeAI

### Полезные шпаргалки

**Быстрый старт:**
```bash
npm install -g @anthropic-ai/claude-code
cd your-project
claude
/init
```

**Частые команды:**
```
/help          - справка
/clear         - очистить контекст
/compact       - сжать контекст
/cost          - использование токенов
/model         - переключить модель
Shift+Tab      - авто-принятие изменений
Escape (x2)    - откат изменений
```

**Примеры запросов:**
```
> объясни архитектуру проекта
> найди баги в @api/users.ts
> добавь тесты для auth модуля
> оптимизируй производительность
> создай pull request
> разреши конфликты слияния
```

---

**Версия документа:** 2.0
**Последнее обновление:** Ноябрь 2025
**Автор:** Сообщество Claude Code (русская локализация)

Если вы нашли ошибку или хотите улучшить документацию, создайте issue или pull request!

Удачного программирования!
