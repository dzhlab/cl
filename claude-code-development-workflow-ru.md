# Как правильно разрабатывать программы с Claude Code

## Полное руководство по эффективной разработке

---

## Содержание

1. [Философия работы с Claude Code](#философия-работы-с-claude-code)
2. [Подготовка проекта](#подготовка-проекта)
3. [Этапы разработки](#этапы-разработки)
4. [Режимы работы](#режимы-работы)
5. [Управление контекстом](#управление-контекстом)
6. [Test-Driven Development с Claude](#test-driven-development-с-claude)
7. [Работа с большими проектами](#работа-с-большими-проектами)
8. [Лайфхаки и трюки](#лайфхаки-и-трюки)
9. [Антипаттерны](#антипаттерны)
10. [Примеры промптов](#примеры-промптов)
11. [Автоматизация workflow](#автоматизация-workflow)
12. [Чеклисты](#чеклисты)

---

## Философия работы с Claude Code

### Главный принцип

> **Claude Code — это не замена программиста, а усилитель ваших навыков.**

Claude Code работает лучше всего, когда вы:
- Чётко понимаете, что хотите получить
- Даёте контекст перед задачей
- Проверяете и направляете его работу
- Итерируете небольшими шагами

### Что Claude делает хорошо

✅ Рутинные задачи (CRUD, бойлерплейт)  
✅ Рефакторинг по чётким правилам  
✅ Написание тестов  
✅ Документирование кода  
✅ Исправление багов с понятным стек-трейсом  
✅ Миграция между технологиями  
✅ Code review  

### Где нужен ваш контроль

⚠️ Архитектурные решения  
⚠️ Бизнес-логика  
⚠️ Безопасность  
⚠️ Производительность критических участков  
⚠️ Интеграции со сторонними сервисами  

---

## Подготовка проекта

### Шаг 1: Инициализация CLAUDE.md

Перед началом работы создайте файл контекста:

```bash
cd /path/to/project
claude
/init
```

### Шаг 2: Структура CLAUDE.md

```markdown
# Название проекта

## Обзор
Краткое описание: что делает проект, для кого, какую проблему решает.

## Технологический стек
- Frontend: React 18, TypeScript, TailwindCSS
- Backend: Node.js, Express, Prisma
- База данных: PostgreSQL
- Тестирование: Jest, Playwright
- CI/CD: GitHub Actions

## Структура проекта
```
src/
├── components/    # React-компоненты
├── hooks/         # Кастомные хуки
├── services/      # API-вызовы
├── utils/         # Утилиты
└── types/         # TypeScript типы
```

## Команды разработки
- `npm run dev` — запуск dev-сервера
- `npm run test` — запуск тестов
- `npm run lint` — проверка кода
- `npm run build` — сборка

## Соглашения о коде
- Используем функциональные компоненты с хуками
- Все функции типизированы
- Тесты обязательны для новой логики
- Коммиты в формате Conventional Commits

## Текущие задачи
- [ ] Реализовать авторизацию
- [ ] Добавить dashboard
- [ ] Оптимизировать запросы к БД
```

### Шаг 3: Файл ROADMAP.md (опционально)

Для крупных проектов создайте roadmap:

```markdown
# Roadmap проекта

## Workflow разработки
1. Изучить существующий код
2. Обновить ROADMAP.md с новой задачей
3. Создать план реализации
4. Реализовать по шагам
5. Написать тесты
6. Сделать код-ревью
7. Закоммитить изменения

## Завершённые задачи
- [x] Настройка проекта
- [x] Базовая структура

## Текущий спринт
- [ ] Авторизация через OAuth
  - [ ] Интеграция с Google
  - [ ] Интеграция с GitHub
  - [ ] Refresh токены

## Бэклог
- [ ] Уведомления
- [ ] Экспорт данных
```

---

## Этапы разработки

### Цикл разработки фичи

```
┌─────────────────────────────────────────────────────────────┐
│  1. ПЛАНИРОВАНИЕ  →  2. РЕАЛИЗАЦИЯ  →  3. ТЕСТИРОВАНИЕ     │
│         ↑                                      │            │
│         └──────────── 4. РЕВЬЮ ←───────────────┘            │
└─────────────────────────────────────────────────────────────┘
```

---

### Этап 1: Планирование (обязательно!)

> ⚡ **Критически важно:** Без планирования Claude часто уходит не туда.

#### Шаг 1.1: Исследование кодовой базы

```
> Прочитай файлы в директории src/auth/ и объясни, 
> как сейчас работает авторизация.
> НЕ ПИШИ КОД, только проанализируй.
```

#### Шаг 1.2: Создание плана

```
> Мне нужно добавить OAuth авторизацию через Google.
> 
> Создай детальный план реализации:
> 1. Какие файлы нужно изменить
> 2. Какие новые файлы создать
> 3. Какие зависимости добавить
> 4. Порядок выполнения шагов
>
> НЕ ПИШИ КОД. Только план.
```

#### Шаг 1.3: Утверждение плана

- Просмотрите план
- Внесите корректировки
- Сохраните план в файл:

```
> Сохрани этот план в docs/oauth-implementation-plan.md
```

---

### Этап 2: Реализация

#### Золотое правило: один шаг за раз

❌ **Плохо:**
```
> Реализуй полностью OAuth авторизацию
```

✅ **Хорошо:**
```
> Реализуй шаг 1 из плана: создай модель User в Prisma
> После завершения остановись и дождись моего подтверждения
```

#### Итеративный подход

```
# Сессия 1: Модели данных
> Создай схему базы данных для OAuth (users, sessions, accounts)
> Запусти миграции и убедись, что они работают

# /clear

# Сессия 2: API endpoints  
> Открой план в docs/oauth-implementation-plan.md
> Реализуй API endpoint для инициации OAuth flow
> Напиши тест для этого endpoint

# /clear

# Сессия 3: Frontend
> Реализуй компонент LoginButton с OAuth
> Проверь интеграцию с backend
```

#### Контрольные точки

После каждого значимого изменения:

```
> Запусти тесты и покажи результат
> Если всё ок — закоммить с сообщением "feat(auth): add Google OAuth endpoint"
```

---

### Этап 3: Тестирование

#### Запрос на написание тестов

```
> Напиши юнит-тесты для модуля src/services/auth.ts
> Покрой следующие кейсы:
> - Успешная авторизация
> - Неверные credentials
> - Истёкший токен
> - Отсутствие пользователя
```

#### Проверка покрытия

```
> Запусти npm run test:coverage
> Проанализируй отчёт и предложи, какие тесты добавить
```

---

### Этап 4: Code Review

```
> Выполни код-ревью изменений с последнего коммита:
> 
> 1. Проверь соответствие code style
> 2. Найди потенциальные баги
> 3. Оцени безопасность
> 4. Проверь обработку ошибок
> 5. Оцени производительность
>
> Выведи список замечаний с приоритетами
```

---

## Режимы работы

### Normal Mode (по умолчанию)

Claude запрашивает подтверждение перед каждым изменением.

**Когда использовать:** Критичные изменения, работа с production-кодом

### Auto-Accept Mode (Shift+Tab)

Claude применяет изменения без подтверждения.

**Когда использовать:** Рутинные задачи, рефакторинг, написание тестов

```
# Переключение режима
Shift+Tab → ⏵⏵ accept edits on
```

### Plan Mode (Shift+Tab дважды)

Claude только планирует, не изменяет код.

**Когда использовать:**
- Начало новой фичи
- Исследование кодовой базы
- Сложные архитектурные решения

```
# Глубина анализа
> think — базовый анализ
> think hard — углублённый анализ
> ultrathink — максимальная глубина
```

### Headless Mode (-p флаг)

Для автоматизации и CI/CD.

```bash
# Линтинг через Claude
claude -p "проверь код на ошибки и выведи список проблем"

# Генерация документации
claude -p "сгенерируй JSDoc для всех функций в src/utils/"
```

---

## Управление контекстом

### Проблема контекста

Claude эффективно работает **10-20 минут** в одной сессии. После этого:
- Контекст переполняется
- Качество ответов падает
- Увеличивается расход токенов

### Стратегия "Одна задача — одна сессия"

```
# Задача 1: Рефакторинг
> Отрефактори компонент UserProfile
> Закоммить изменения
/clear

# Задача 2: Тесты
> Напиши тесты для UserProfile
> Закоммить
/clear

# Задача 3: Документация
> Задокументируй UserProfile
```

### Когда использовать /clear vs /compact

| Ситуация | Команда |
|----------|---------|
| Новая задача | `/clear` |
| Продолжение текущей задачи | `/compact` |
| Много ненужного контекста | `/clear` |
| Нужно сохранить решения | `/compact [что сохранить]` |

### Пример /compact

```
/compact Сохрани:
- Схему базы данных
- Решение по архитектуре авторизации
- Список уже реализованных endpoints
```

### Мониторинг контекста

```
# Проверить использование контекста
/context

# Проверить расход токенов
/cost
```

---

## Test-Driven Development с Claude

> 🎯 **TDD — самый эффективный паттерн работы с Claude Code**

### Почему TDD работает с ИИ

1. Тесты создают чёткие границы задачи
2. Уменьшают "галлюцинации" и отклонения
3. Позволяют итеративно проверять прогресс
4. Claude отлично понимает fail → pass цикл

### Workflow TDD

```
# Шаг 1: Напиши тесты
> Напиши тесты для функции calculateDiscount:
> - Скидка 10% при сумме > 1000
> - Скидка 20% при сумме > 5000
> - Без скидки при сумме < 1000
> 
> НЕ ПИШИ РЕАЛИЗАЦИЮ. Только тесты.
> Используй моки где нужно.

# Шаг 2: Проверь, что тесты падают
> Запусти тесты и убедись, что они падают

# Шаг 3: Закоммить тесты
> Закоммить тесты: "test: add calculateDiscount tests"

# /clear — новая сессия для реализации

# Шаг 4: Реализация
> Открой тесты в src/utils/discount.test.ts
> Реализуй функцию calculateDiscount так, чтобы все тесты прошли
> После реализации запусти тесты

# Шаг 5: Рефакторинг (если нужно)
> Отрефактори calculateDiscount, сохраняя тесты зелёными
```

### Пример: создание API endpoint

```
# Тесты первыми
> Создай интеграционные тесты для POST /api/orders:
> - Успешное создание заказа (201)
> - Валидация обязательных полей (400)
> - Неавторизованный запрос (401)
> - Несуществующий товар (404)
>
> Используй supertest. Не пиши реализацию endpoint.

# После коммита тестов
/clear

# Реализация
> Реализуй POST /api/orders чтобы все тесты прошли
```

---

## Работа с большими проектами

### Декомпозиция на этапы

Для проектов, которые не влезают в один контекст:

```
# Этап 1: Спецификация
> Создай spec.md с полным описанием проекта:
> - Функциональные требования
> - Технический стек
> - Архитектура
> - API endpoints
> - Схема БД

# Этап 2: План промптов
> На основе spec.md создай prompt_plan.md:
> Разбей реализацию на 10-15 атомарных задач
> Каждая задача должна занимать не более 20 минут
> Отметь зависимости между задачами

# Этап 3: Последовательная реализация
> Открой @prompt_plan.md
> Найди первую незавершённую задачу
> Реализуй её
> Убедись, что тесты проходят
> Закоммить
> Отметь задачу как завершённую
```

### Параллельная работа с Git Worktrees

```bash
# Создаём отдельные рабочие директории
git worktree add ../project-feature-auth feature/auth
git worktree add ../project-feature-dashboard feature/dashboard

# В терминале 1
cd ../project-feature-auth
claude
> Реализуй систему авторизации

# В терминале 2
cd ../project-feature-dashboard  
claude
> Создай dashboard компонент
```

### Субагенты для специализированных задач

```
# Создаём субагента для ревью
> /agents

# Или через файл .claude/agents/security-reviewer.md
---
name: security-reviewer
description: Проверяет код на уязвимости. Вызывай после изменений в auth/.
tools: Read, Grep, Glob
model: opus
---

Ты — эксперт по безопасности. Проверяй:
- SQL-инъекции
- XSS
- CSRF
- Проблемы с аутентификацией
- Утечки данных

Выводи отчёт с severity: critical/high/medium/low
```

Использование:
```
@security-reviewer проверь изменения в src/auth/
```

---

## Лайфхаки и трюки

### 1. Используй LLM для генерации промптов

```
# Сначала в обычном Claude или ChatGPT:
"Я хочу создать систему уведомлений. 
Сгенерируй мне детальный промпт для Claude Code, 
который разобьёт эту задачу на шаги."
```

### 2. Предоставляй ошибки полностью

❌ **Плохо:**
```
> Авторизация не работает, почини
```

✅ **Хорошо:**
```
> Авторизация падает с ошибкой:
> 
> Error: JWT malformed
>   at verify (/app/node_modules/jsonwebtoken/verify.js:75:17)
>   at /app/src/middleware/auth.ts:23:5
>
> Ожидаемое: пользователь авторизуется и получает токен
> Фактическое: 401 ошибка сразу после логина
>
> Вот код middleware:
> [вставь код]
```

### 3. Используй скриншоты для UI

```
> [вставляешь скриншот через Ctrl+V]
> Видишь этот баг с расположением кнопки?
> Исправь CSS так, чтобы кнопка была справа
```

### 4. Tab-автодополнение для файлов

Вместо написания полных путей:
```
> Открой src/com[Tab]User[Tab]
→ src/components/UserProfile/
```

### 5. Прерывай, если что-то идёт не так

```
[Escape] — остановить
> Отмени последние изменения
> Давай попробуем другой подход...
```

### 6. Rewind для отката

```
[Escape][Escape] — или /rewind
→ Выбираешь точку, к которой откатиться
```

### 7. Фоновые команды

```
# Запустить dev-сервер в фоне
Ctrl+B → npm run dev

# Claude продолжает работать, пока сервер запущен
```

### 8. История сообщений

```
↑ — предыдущие сообщения текущей сессии
↑↑↑ — можно найти сообщения из прошлых сессий
```

### 9. Быстрые bash-команды

```
!git status
!npm run test
!cat src/config.ts
```

### 10. Пайпы для контекста

```bash
# Передать ошибки напрямую
npm run build 2>&1 | claude -p "объясни эти ошибки и исправь"

# Передать diff
git diff | claude -p "сделай code review этих изменений"

# Передать логи
tail -100 /var/log/app.log | claude -p "найди причину падений"
```

### 11. Шаблоны в CLAUDE.md

```markdown
## Шаблоны ответов

При создании компонента следуй шаблону:
- Файл компонента: ComponentName.tsx
- Файл стилей: ComponentName.module.css
- Файл тестов: ComponentName.test.tsx
- Файл stories: ComponentName.stories.tsx
```

### 12. Настрой разрешения заранее

```json
// .claude/settings.json
{
  "permissions": {
    "allowedTools": [
      "Read",
      "Write(src/**)",
      "Write(tests/**)",
      "Bash(npm *)",
      "Bash(git *)",
      "Bash(npx *)"
    ],
    "deny": [
      "Write(.env*)",
      "Bash(rm -rf *)",
      "Bash(sudo *)"
    ]
  }
}
```

---

## Антипаттерны

### ❌ Не делай так

#### 1. Задачи "в один промпт"

```
❌ > Создай полный e-commerce сайт с корзиной, оплатой и админкой
```

**Почему плохо:** Слишком большой scope, Claude потеряется

**Как правильно:** Разбей на 20+ мелких задач

---

#### 2. Отсутствие контекста

```
❌ > Почини баг
```

**Как правильно:**
```
✅ > В файле src/cart.ts функция calculateTotal возвращает NaN
> когда в корзине товар с quantity: 0
> Вот стек-трейс: [...]
```

---

#### 3. Игнорирование тестов

```
❌ > Добавь фичу X
❌ > Теперь добавь фичу Y
❌ > Теперь Z
// 3 часа спустя: всё сломано, непонятно где
```

**Как правильно:**
```
✅ > Добавь фичу X
> Напиши тесты
> Запусти, убедись что зелёные
> Закоммить
/clear
```

---

#### 4. Бесконечные сессии

```
❌ // 2 часа в одной сессии
// Контекст переполнен
// Claude путается и противоречит себе
```

**Как правильно:** Новая задача = `/clear` = новая сессия

---

#### 5. Слепое доверие

```
❌ // Claude сделал — значит правильно
// Не смотрю код, не запускаю тесты
```

**Как правильно:** Всегда проверяй изменения, запускай тесты

---

#### 6. Skip всех разрешений на production

```
❌ claude --dangerously-skip-permissions  // на prod-сервере
```

**Когда допустимо:** Только в изолированном dev-окружении с git

---

## Примеры промптов

### Начало проекта

```
> Я хочу создать [описание проекта].
> 
> Сначала создай детальную спецификацию:
> 1. Функциональные требования
> 2. Технический стек (предложи оптимальный)
> 3. Структура проекта
> 4. Схема базы данных
> 5. API endpoints
> 
> Сохрани в spec.md. Не пиши код.
```

### Исследование кодовой базы

```
> Изучи этот проект и объясни:
> 1. Общую архитектуру
> 2. Как данные проходят от UI до БД
> 3. Какие паттерны используются
> 4. Потенциальные проблемы
>
> Не меняй код, только анализируй.
```

### Рефакторинг

```
> Отрефактори @src/services/userService.ts:
> 
> Требования:
> - Разбей на более мелкие функции
> - Добавь типизацию
> - Улучши обработку ошибок
> - Сохрани обратную совместимость
>
> После рефакторинга запусти тесты.
```

### Добавление фичи

```
> Добавь функционал экспорта данных в CSV.
>
> Контекст:
> - Данные хранятся в @src/stores/dataStore.ts
> - UI для кнопки добавь в @src/components/DataTable.tsx
> - Используй существующий стиль проекта
>
> Шаги:
> 1. Создай утилиту для конвертации в CSV
> 2. Добавь кнопку в UI
> 3. Напиши тесты
> 4. Закоммить каждый шаг отдельно
```

### Исправление бага

```
> Баг: при сортировке таблицы по дате происходит неправильная сортировка
>
> Репродукция:
> 1. Открыть /dashboard
> 2. Нажать на заголовок "Дата"
> 3. Даты сортируются как строки, не как даты
>
> Ожидаемое: хронологическая сортировка
> Фактическое: алфавитная сортировка ("10 января" < "2 января")
>
> Найди проблему и исправь.
```

### Code review

```
> Проведи code review изменений:
> !git diff main...HEAD
>
> Проверь:
> 1. Логические ошибки
> 2. Уязвимости безопасности
> 3. Производительность
> 4. Соответствие стилю проекта
> 5. Покрытие тестами
>
> Выведи список замечаний с severity: critical/major/minor
```

### Документация

```
> Создай документацию для API в формате OpenAPI/Swagger:
> - Изучи все endpoints в src/routes/
> - Определи request/response схемы
> - Добавь примеры
> - Сохрани в docs/api.yaml
```

---

## Автоматизация workflow

### Custom slash-команды

#### /feature — начало новой фичи

```markdown
<!-- .claude/commands/feature.md -->
---
argument-hint: [название-фичи]
description: Начать разработку новой фичи
---

Начинаем разработку фичи: $ARGUMENTS

1. Создай новую ветку: feature/$ARGUMENTS
2. Изучи связанные файлы в кодовой базе
3. Создай план реализации в docs/features/$ARGUMENTS.md
4. Дождись моего подтверждения плана
```

#### /commit — умный коммит

```markdown
<!-- .claude/commands/commit.md -->
---
description: Создать коммит с описательным сообщением
---

1. Запусти тесты: npm run test
2. Если тесты падают — исправь и запусти снова
3. Запусти линтер: npm run lint
4. Создай коммит с сообщением в формате Conventional Commits
5. Покажи git log -1 для проверки
```

#### /review — автоматическое ревью

```markdown
<!-- .claude/commands/review.md -->
---
description: Code review изменений
---

Выполни code review:

1. !git diff main...HEAD
2. Проверь на:
   - Баги и логические ошибки
   - Уязвимости безопасности  
   - Проблемы производительности
   - Соответствие стилю кода
   - Наличие тестов

3. Выведи отчёт с severity каждого замечания
4. Предложи исправления для critical и major
```

### Хуки

```json
// .claude/settings.json
{
  "hooks": {
    "preEditAccept": [
      "prettier --write $FILE"
    ],
    "postEditAccept": [
      "eslint --fix $FILE",
      "npx tsc --noEmit"
    ]
  }
}
```

### CI/CD интеграция

```yaml
# .github/workflows/claude-review.yml
name: Claude Code Review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          prompt: |
            Проведи code review этого PR.
            Проверь на баги, уязвимости, проблемы производительности.
            Комментируй только важные замечания.
```

---

## Чеклисты

### Чеклист начала проекта

- [ ] Создать CLAUDE.md с описанием проекта
- [ ] Настроить .claude/settings.json с разрешениями
- [ ] Создать базовые slash-команды
- [ ] Настроить хуки для линтинга
- [ ] Добавить ROADMAP.md для планирования

### Чеклист перед фичей

- [ ] Очистить контекст: `/clear`
- [ ] Изучить текущий код (без изменений)
- [ ] Создать план реализации
- [ ] Утвердить план
- [ ] Разбить на мелкие задачи

### Чеклист после изменений

- [ ] Запустить тесты
- [ ] Запустить линтер
- [ ] Провести code review
- [ ] Проверить изменения вручную
- [ ] Создать осмысленный коммит

### Чеклист перед merge

- [ ] Все тесты проходят
- [ ] Code review пройден
- [ ] Документация обновлена
- [ ] CHANGELOG обновлён
- [ ] Нет конфликтов с main

---

## Метрики эффективности

### Что измерять

| Метрика | Цель | Как измерить |
|---------|------|--------------|
| Время на фичу | -30% | Трекинг задач |
| Покрытие тестами | >80% | `npm run coverage` |
| Баги в production | -50% | Bug tracker |
| Размер PR | <300 строк | GitHub stats |
| Время code review | -40% | PR metrics |

### Реалистичные ожидания

- **10-30%** прирост продуктивности при правильном workflow
- **Больше** для рутинных задач (бойлерплейт, тесты, документация)
- **Меньше** для сложной бизнес-логики и архитектуры

---

## Заключение

### Ключевые принципы

1. **Планируй перед кодом** — заставляй Claude анализировать и планировать
2. **Маленькие итерации** — одна задача, один коммит, один `/clear`
3. **TDD** — тесты первыми, Claude их обожает
4. **Контекст решает** — хороший CLAUDE.md = хорошие результаты
5. **Доверяй, но проверяй** — всегда ревьюй код Claude
6. **Автоматизируй рутину** — slash-команды, хуки, CI/CD

### Формула успеха

```
Чёткая задача + Контекст + Маленькие шаги + Тесты + Ревью = 🚀
```

Удачной разработки!
