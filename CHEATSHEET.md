# Claude Code — Шпаргалка

Быстрый справочник по командам и возможностям Claude Code.

## 🚀 Быстрый старт

```bash
# Установка
npm install -g @anthropic-ai/claude-code

# Запуск в проекте
cd your-project
claude

# Первая настройка
/init
```

## 📝 Основные команды CLI

```bash
# Интерактивный режим
claude

# Headless режим (вывод в терминал)
claude -p "твой запрос"

# Выбор модели
claude --model sonnet    # Быстро и дешево
claude --model opus      # Медленно но умно

# Добавить директории в контекст
claude --add-dir ../frontend --add-dir ../backend

# JSON вывод
claude -p "запрос" --output-format json

# Без запросов разрешений (осторожно!)
claude --dangerously-skip-permissions

# Диагностика
claude doctor
```

## 💬 Slash-команды

### Управление сессией
```
/help          - Показать все команды
/init          - Создать CLAUDE.md
/clear         - Очистить контекст
/compact       - Сжать контекст
/resume        - Возобновить прошлую сессию
/rewind        - Откатить изменения
/export        - Экспортировать разговор
/exit, /quit   - Выйти
```

### Настройки
```
/config        - Открыть настройки
/permissions   - Управление разрешениями
/model         - Переключить модель (Sonnet/Opus)
/cost          - Показать использование токенов
/context       - Визуализация контекста
/login         - Сменить аккаунт
```

### Интеграции
```
/mcp           - Управление MCP-серверами
/hooks         - Настроить хуки
/agents        - Управление субагентами
/install-github-app - GitHub App для ревью PR
```

### Выполнение команд
```
!git status    - Выполнить bash команду
!npm test
!ls -la
```

## ⌨️ Горячие клавиши

```
Enter          - Отправить сообщение
Escape         - Прервать операцию
Escape (x2)    - Rewind — откат изменений
Tab            - Автодополнение файлов
Shift+Tab      - Переключить авто-принятие
Control+V      - Вставить изображение
Control+C      - Выход
Control+Z      - Приостановить (fg для возобновления)
Control+U      - Отменить ввод
Control+B      - Запустить команду в фоне
↑ / ↓          - История сообщений
```

## 📂 Работа с файлами

```
# Ссылка на файл
> Прочитай @api/users.ts

# Несколько файлов
> Сравни @old/auth.js и @new/auth.ts

# Перетащить файл
Shift + перетащить файл в терминал

# Вставить изображение
Control+V (скриншот/изображение)
```

## 🎯 Эффективные запросы

### ❌ Плохо
```
> исправь это
> оптимизируй
> добавь тесты
```

### ✅ Хорошо
```
> В файле @api/orders.ts функция createOrder не валидирует
> inventory. Добавь проверку наличия товара и верни 400
> если товара нет.

> Функция calculateShipping в @utils/shipping.ts работает 200ms.
> Оптимизируй используя кеширование для стандартных маршрутов.

> Добавь integration тесты для auth flow:
> регистрация → подтверждение email → вход → обновление токена
```

## 🔧 Управление контекстом

```
# Очистить полностью
/clear

# Сжать с сохранением важного
/compact Сохрани архитектуру API и схему БД

# Проверить размер
/context

# Разбивай большие задачи
> Реализуй базовую JWT аутентификацию
/compact
> Добавь OAuth провайдеры
/compact
> Добавь 2FA
```

## 🔐 Управление разрешениями

### Файл .claude/settings.json
```json
{
  "permissions": {
    "allowedTools": [
      "Read",
      "Write(src/**)",
      "Bash(git *)",
      "Bash(npm *)"
    ],
    "deny": [
      "Bash(rm -rf *)",
      "Bash(sudo *)",
      "Write(.env*)",
      "Write(secrets/*)"
    ]
  }
}
```

### Файл .claudeignore
```
node_modules/
.env
.env.*
*.key
*.pem
secrets/
dist/
build/
```

## 🤖 MCP Серверы

```bash
# Добавить
claude mcp add <имя> <команда> [args...]

# Примеры
claude mcp add postgres -- npx -y @modelcontextprotocol/server-postgres

claude mcp add github -- npx -y @modelcontextprotocol/server-github \
  --env GITHUB_TOKEN=$GITHUB_TOKEN

# Список
claude mcp list

# Удалить
claude mcp remove <имя>
```

## 🎭 Субагенты

### Создать агента
`.claude/agents/code-reviewer.md`:
```markdown
---
name: code-reviewer
description: Эксперт по код-ревью
tools: Read, Grep, Bash
model: sonnet
---

Ты старший разработчик. Проверяй код на:
- Качество
- Безопасность
- Производительность
```

### Использовать
```
@code-reviewer проверь изменения в auth модуле
```

## 🔄 Хуки

### .claude/settings.json
```json
{
  "hooks": {
    "sessionStart": ["git fetch origin"],
    "preEditAccept": ["prettier --write $FILE"],
    "postEditAccept": ["eslint $FILE --fix"]
  }
}
```

## 📦 Git интеграция

```
# Создать ветку
> создай ветку feature-auth

# Коммит
> закоммить изменения с понятным сообщением

# Pull Request
> создай pull request для этих изменений

# Разрешить конфликты
> разреши конфликты слияния

# Release notes
> сгенерируй release notes из последних 10 коммитов
```

## 🧪 Тестирование

```
# Создать тесты
> Создай тесты для @utils/validators.ts

# Запустить тесты
> Запусти тесты

# Запустить и исправить падающие
> Запусти тесты и исправь все падающие

# Проверить покрытие
> Проверь покрытие тестами и добавь недостающие
```

## 📊 Примеры использования

### Рефакторинг
```
> Отрефактори @api/legacy-auth.js используя современные практики:
> - async/await вместо callbacks
> - TypeScript типы
> - Разбей на мелкие функции
> - Добавь обработку ошибок
```

### Документация
```
> Создай JSDoc документацию для всех функций в @utils/helpers.ts
> Добавь примеры использования в комментариях
```

### Code Review
```
> Сделай ревью @api/payments.ts на предмет:
> - Безопасности (injection, validation)
> - Обработки ошибок
> - Производительности
> - Читаемости кода
```

### Отладка
```
> У меня ошибка в @api/users.ts:45
> [вставляете stack trace]
>
> Ожидаемое: возврат пользователя
> Фактическое: null
>
> Найди и исправь проблему
```

### Оптимизация
```
> Страница @pages/catalog.tsx загружается 3 секунды.
> Профайл показывает что компонент ProductList рендерится 50 раз.
> Оптимизируй используя React.memo и useMemo
```

## 🎨 Пользовательские команды

### .claude/commands/deploy.md
```markdown
---
description: Деплой на staging
argument-hint: [staging|production]
---

Выполни деплой на $1:
1. Запусти тесты
2. Собери проект
3. Задеплой через npm run deploy:$1
4. Запусти smoke tests
5. Уведоми в Slack
```

### Использование
```
/deploy staging
```

## 💡 Лучшие практики

### DO
✅ Используй `/clear` при переключении задач
✅ Пиши конкретные запросы с контекстом
✅ Указывай конкретные файлы через @
✅ Создай CLAUDE.md для проекта
✅ Настрой .claudeignore
✅ Разбивай большие задачи
✅ Проверяй изменения перед коммитом

### DON'T
❌ Не используй расплывчатые запросы
❌ Не накапливай контекст бесконечно
❌ Не коммить без ревью изменений
❌ Не хранить секреты в коде
❌ Не использовать --dangerously-skip-permissions в продакшене

## 🚨 Troubleshooting

```bash
# Проблемы с установкой
npm cache clean --force
npm install -g @anthropic-ai/claude-code

# Проблемы с аутентификацией
/login

# Медленная работа
# 1. Проверь .claudeignore
# 2. Используй /compact
# 3. Очисти контекст /clear

# Ошибки Node.js
node --version  # Должно быть 18+
nvm install 18
nvm use 18

# Диагностика
claude doctor
```

## 📚 Полезные ресурсы

- **Документация:** https://docs.claude.com/claude-code
- **GitHub:** https://github.com/anthropics/claude-code
- **MCP Servers:** https://github.com/modelcontextprotocol/servers
- **Сообщество:** Discord, Reddit r/ClaudeAI

## 💰 Оптимизация стоимости

```
# Используй Sonnet для простых задач
claude --model sonnet

# Очищай контекст часто
/compact
/clear

# Создай хороший CLAUDE.md
# (меньше файлов нужно читать)

# Используй .claudeignore
# (меньше файлов в контексте)

# Пиши конкретные запросы
# (меньше итераций)

# Headless для автоматизации
git diff | claude -p "найди баги" > report.txt
```

## 🎯 Горячие советы

1. **Начни с `/init`** - создай CLAUDE.md для проекта
2. **Используй @файл** - явно указывай какие файлы нужны
3. **Контекст важен** - предоставляй детали при ошибках
4. **Разбивай задачи** - большую задачу дели на маленькие
5. **Проверяй изменения** - всегда ревьюй что изменил Claude
6. **Escape - твой друг** - не бойся прерывать операции
7. **Коммить часто** - маленькие коммиты лучше больших
8. **Создавай команды** - автоматизируй частые задачи
9. **Используй субагентов** - для специализированных задач
10. **Читай документацию** - там много полезного!

---

**Версия:** 2.0
**Дата:** Ноябрь 2025
