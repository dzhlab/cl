---
description: Анализ производительности приложения
argument-hint: [файл или компонент]
---

Проанализируй производительность $1 и предложи оптимизации

## Что анализировать:

### 1. Производительность кода
- Сложность алгоритмов (O-notation)
- Ненужные вычисления
- Множественные проходы по данным
- Блокирующие операции

### 2. Работа с данными
- **N+1 запросы к БД**
  ```javascript
  // ❌ Плохо - N+1 проблема
  users.forEach(user => {
    const posts = db.getPosts(user.id); // N запросов
  });

  // ✅ Хорошо - один запрос
  const userIds = users.map(u => u.id);
  const posts = db.getPostsByUserIds(userIds); // 1 запрос
  ```

- **Отсутствие пагинации**
- **Загрузка лишних данных**
- **Отсутствие индексов**

### 3. Frontend (React/Vue/Angular)
- Ненужные ререндеры
- Отсутствие мемоизации
- Большие бандлы
- Неоптимизированные изображения
- Отсутствие ленивой загрузки

### 4. Память
- Утечки памяти
- Неочищенные подписки
- Большие объекты в замыканиях
- Кеш без TTL

### 5. Сеть
- Множественные HTTP запросы
- Отсутствие кеширования
- Большой размер ответов
- Нет сжатия (gzip/brotli)

## Инструменты для анализа:

### JavaScript/Node.js
```bash
# Профилирование
node --prof app.js
node --prof-process isolate-*.log

# Chrome DevTools
# Во время работы приложения:
# 1. Performance tab
# 2. Записать профиль
# 3. Анализировать результаты

# Memory leaks
node --inspect app.js
# Chrome: chrome://inspect
```

### React
```javascript
// React DevTools Profiler
import { Profiler } from 'react';

function onRenderCallback(id, phase, actualDuration) {
  console.log(`${id} took ${actualDuration}ms to render`);
}

<Profiler id="MyComponent" onRender={onRenderCallback}>
  <MyComponent />
</Profiler>
```

### Python
```bash
# cProfile
python -m cProfile -o output.prof script.py
python -m pstats output.prof

# line_profiler
pip install line_profiler
@profile
def my_function():
    # код

kernprof -l -v script.py
```

### База данных
```sql
-- PostgreSQL
EXPLAIN ANALYZE SELECT * FROM users WHERE email = 'test@example.com';

-- Проверка медленных запросов
SELECT query, calls, total_time, mean_time
FROM pg_stat_statements
ORDER BY total_time DESC
LIMIT 10;
```

## Типичные проблемы и решения:

### Проблема 1: Медленные запросы к БД

**Находим:**
```javascript
// Запрос выполняется 500ms
const user = await db.query('SELECT * FROM users WHERE email = ?', [email]);
```

**Решение:**
```sql
-- Добавляем индекс
CREATE INDEX idx_users_email ON users(email);
```

**Результат:** 500ms → 5ms

### Проблема 2: React компонент рендерится слишком часто

**Находим:**
```javascript
function UserList({ users }) {
  return users.map(user => <UserCard key={user.id} user={user} />);
}
// Рендерится при каждом изменении родителя
```

**Решение:**
```javascript
const UserList = React.memo(function UserList({ users }) {
  return users.map(user => <UserCard key={user.id} user={user} />);
});

const UserCard = React.memo(function UserCard({ user }) {
  // ...
});
```

### Проблема 3: Загрузка всех данных сразу

**Находим:**
```javascript
// Загружаем 10,000 записей
const allUsers = await fetchAllUsers();
```

**Решение:**
```javascript
// Пагинация
const users = await fetchUsers({ page: 1, limit: 50 });

// Бесконечная прокрутка
const { data, fetchMore } = useInfiniteQuery('users', fetchUsers);

// Виртуализация (react-window)
import { FixedSizeList } from 'react-window';
```

### Проблема 4: Большой размер бандла

**Находим:**
```bash
npm run build
# main.js 2.5 MB
```

**Решение:**
```javascript
// Ленивая загрузка
const HeavyComponent = React.lazy(() => import('./HeavyComponent'));

// Code splitting
<Suspense fallback={<Spinner />}>
  <HeavyComponent />
</Suspense>

// Tree shaking - импорт только нужного
import debounce from 'lodash/debounce'; // ✅ 2KB
// вместо
import { debounce } from 'lodash'; // ❌ 70KB
```

### Проблема 5: Утечка памяти

**Находим:**
```javascript
useEffect(() => {
  const interval = setInterval(() => fetchData(), 1000);
  // Не очищаем!
}, []);
```

**Решение:**
```javascript
useEffect(() => {
  const interval = setInterval(() => fetchData(), 1000);

  return () => clearInterval(interval); // ✅ Cleanup
}, []);
```

## Метрики для отслеживания:

### Web Vitals
- **LCP** (Largest Contentful Paint) — < 2.5s
- **FID** (First Input Delay) — < 100ms
- **CLS** (Cumulative Layout Shift) — < 0.1

### Backend
- **Response time** — p50, p95, p99
- **Throughput** — requests/second
- **Error rate** — < 1%
- **Database query time** — < 100ms

### Frontend
- **Bundle size** — < 200KB (gzipped)
- **Time to Interactive** — < 3s
- **First Contentful Paint** — < 1s

## Процесс оптимизации:

1. **Измерить** — используй профайлер
2. **Найти узкие места** — что занимает больше всего времени?
3. **Приоритизировать** — что даст максимальный эффект?
4. **Оптимизировать** — внести изменения
5. **Измерить снова** — убедиться в улучшении
6. **Повторить** — пока не достигнут приемлемый уровень

## Что делать:

1. Прочитай код в $1
2. Запусти профайлинг если доступно
3. Найди проблемы производительности
4. Предложи конкретные оптимизации с примерами
5. Оцени ожидаемое улучшение
6. Приоритизируй по impact/effort

## Формат ответа:

```markdown
# Анализ производительности: $1

## Найденные проблемы

### 🔴 Критические (требуют немедленного исправления)
1. **Проблема:** N+1 запросы в getUsersWithPosts
   **Impact:** 5000ms → 50ms (100x улучшение)
   **Effort:** Low
   **Решение:** [код]

### 🟡 Важные
2. **Проблема:** Отсутствие мемоизации в ProductList
   **Impact:** 500ms → 50ms (10x улучшение)
   **Effort:** Low
   **Решение:** [код]

### 🟢 Рекомендации
3. **Проблема:** Можно добавить кеширование
   **Impact:** 200ms → 5ms (40x улучшение)
   **Effort:** Medium
   **Решение:** [код]

## Приоритеты
1. Исправить N+1 запросы (критично)
2. Добавить мемоизацию (важно)
3. Настроить кеширование (желательно)

## Ожидаемый результат
- Текущая производительность: 5.7s
- После оптимизаций: ~0.3s
- Улучшение: ~19x
```
